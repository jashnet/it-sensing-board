name: 🌅 Daily Morning Sensing Batch

on:
  schedule:
    - cron: '0 22 * * *'
  workflow_dispatch:

# 💡 [핵심 추가] 여러 개의 서버가 동시에 달리는 것을 원천 차단 (충돌의 주범 해결!)
concurrency:
  group: morning-sensing
  cancel-in-progress: true

jobs:
  run-batch:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: 저장소 체크아웃
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: 파이썬 환경 설정
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 필수 라이브러리 설치
        run: |
          pip install feedparser google-genai beautifulsoup4 deep-translator

      - name: 모닝 센싱 스크립트 실행 (batch.py)
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python batch.py

      - name: 수집된 결과(JSON)를 Github에 덮어쓰기 저장
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          
          git add today_news.json
          git add archive/*.json || true
          
          # 바뀐 파일이 없으면 억지로 진행하지 않고 평화롭게 종료합니다.
          if git diff-index --quiet HEAD; then
            echo "No changes to commit"
            exit 0
          fi
          
          git commit -m "🤖 [Automated] Update Morning Sensing Data"
          
          # 💡 [안전 장치] 혹시라도 다른 변동사항이 생겼다면 내 데이터 기준으로 강제 병합합니다.
          git pull origin main --no-rebase -X ours --no-edit || true
          
          # 🚀 최종 발송!
          git push origin main
