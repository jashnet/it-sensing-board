name: 🌅 Daily Morning Sensing Batch

on:
  schedule:
    - cron: '0 22 * * *'
  workflow_dispatch:

concurrency:
  group: morning-sensing
  cancel-in-progress: true

jobs:
  run-batch:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: 저장소 체크아웃
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: 파이썬 환경 설정
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 필수 라이브러리 설치
        run: |
          pip install feedparser google-genai beautifulsoup4 deep-translator

      - name: 모닝 센싱 스크립트 실행 (batch.py)
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python batch.py

      - name: 수집된 결과(JSON)를 Github에 덮어쓰기 저장
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          
          git add today_news.json
          git add archive/*.json || true
          
          # 바뀐 게 없으면 여기서 조용히 끝냅니다.
          git commit -m "🤖 [Automated] Update Morning Sensing Data" || exit 0
          
          # 💡 [핵심] 깃허브의 최신 코드를 무조건 먼저 당겨옵니다. (로그에 이 명령어가 꼭 찍혀야 합니다!)
          git pull origin main --rebase
          
          # 🚀 최종 덮어쓰기
          git push origin main
