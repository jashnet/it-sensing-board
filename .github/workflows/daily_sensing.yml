name: 🌅 Daily Morning Sensing Batch

on:
  schedule:
    - cron: '0 22 * * *'
  workflow_dispatch:

jobs:
  run-batch:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: 저장소 체크아웃
        uses: actions/checkout@v4
        with:
          ref: main         # 💡 처음부터 무조건 main 브랜치 기준으로 작업 시작!
          fetch-depth: 0    # 💡 과거 기록을 모두 가져와서 꼬임 방지

      - name: 파이썬 환경 설정
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 필수 라이브러리 설치
        run: |
          pip install feedparser google-genai beautifulsoup4 deep-translator

      - name: 모닝 센싱 스크립트 실행 (batch.py)
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python batch.py

      - name: 수집된 결과(JSON)를 Github에 덮어쓰기 저장
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          
          # 1. 봇이 방금 생성한 파일들만 콕 집어서 추가합니다.
          git add today_news.json
          git add archive/ || true
          
          # 2. 커밋을 시도합니다. (만약 뉴스가 없어서 바뀐 내용이 없다면 여기서 깔끔하게 성공 상태로 종료합니다)
          git commit -m "🤖 [Automated] Update Morning Sensing Data" || exit 0
          
          # 3. 푸시하기 직전에 깃허브의 최신 코드를 당겨와서 병합합니다. (충돌 원천 차단)
          git pull --rebase origin main
          
          # 4. 최종 업로드!
          git push origin main
