name: 🌅 Daily Morning Sensing Batch

on:
  schedule:
    # 한국 시간 기준 매일 아침 7시 (UTC 시간으로는 밤 10시)에 자동 실행
    - cron: '0 22 * * *'
  workflow_dispatch: # 수동으로도 버튼을 눌러 돌릴 수 있게 함

jobs:
  run-batch:
    runs-on: ubuntu-latest
    steps:
      - name: 저장소 체크아웃
        uses: actions/checkout@v3

      - name: 파이썬 환경 설정
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 필수 라이브러리 설치
        run: |
          pip install feedparser google-genai beautifulsoup4 deep-translator

      - name: 모닝 센싱 스크립트 실행 (batch.py)
        env:
          # Github Settings -> Secrets에 저장해둔 GEMINI_API_KEY를 불러옵니다
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python batch.py

      - name: 수집된 결과(JSON)를 Github에 덮어쓰기 저장
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          
          # 1. 일단 가상 컴퓨터에서 만든 뉴스 파일들을 묶어서 저장할 준비를 합니다.
          git add today_news.json || true
          git add archive/*.json || true
          git commit -m "🤖 [Automated] Update Morning Sensing Data" || echo "No changes to commit"
          
          # 2. 💡 [가장 중요] 업로드 직전에 깃허브의 최신 상태를 먼저 당겨와서 병합합니다! (충돌 원천 차단)
          git pull --rebase origin main
          
          # 3. 안전하게 하나로 합쳐진 파일을 최종 업로드합니다.
          git push origin main || echo "No changes to push"
